# 互联网大厂后端面经汇总 Agent 开发文档

## 1. 项目概述

* **项目名称**：Backend-Interview-Scout-Agent
* **项目目标**：利用 LLM 的推理能力和外部工具，自动从互联网（如牛客网、掘金、知乎、CSDN等）检索、读取并总结各大互联网公司（阿里、腾讯、字节跳动等）的后端开发面试经验，最终生成结构化的汇总报告。
* **技术栈**：
  * **语言**：Go 1.21+
  * **核心框架**：`github.com/tmc/langchaingo`
  * **LLM 接入**：OpenAI API (GPT-4o) 或 DeepSeek/通义千问等兼容 OpenAI 格式的模型
  * **搜索引擎 API**：Tavily API 或 SerpApi（用于快速检索网页链接，避免从零写爬虫）
  * **网页解析**：`github.com/PuerkitoBio/goquery`

---

## 2. Agent 架构设计

根据 Agent 的基本定义，本系统包含以下四大核心组件：

1. **大脑 (Brain / LLM)**：负责理解用户需求（如"帮我找一下字节跳动最近一个月的 Go 后端面经"），规划拆解任务，决定调用哪些工具。
2. **工具 (Tools)**：
   * `WebSearchTool`：使用搜索引擎 API 搜索相关面经文章，获取 URL 列表。
   * `WebScrapeTool`：根据 URL 抓取网页正文内容，并去除 HTML 标签等无用信息。
3. **记忆 (Memory)**：使用 `ConversationBufferMemory` 记录多轮对话上下文，以便用户进行追问（如"上面提到的 Redis 缓存击穿问题，有没有更详细的解答？"）。
4. **执行器 (Agent Executor)**：采用 **ReAct (Reason + Act)** 模式，循环执行"思考 -> 行动 -> 观察"的流程，直到得出最终结论。

### 2.1 架构流程图

```
用户输入
  │
  ▼
┌──────────────────────────────────────────┐
│           Agent Executor (ReAct)         │
│                                          │
│  ┌─────────┐   ┌────────┐   ┌────────┐  │
│  │ Thought │──▶│ Action │──▶│Observe │  │
│  │ (推理)  │   │(调工具) │   │(看结果)│  │
│  └────▲────┘   └────────┘   └───┬────┘  │
│       │                         │        │
│       └─────────────────────────┘        │
│              循环直到得出结论              │
│                                          │
│  ┌─────────────────────────────────────┐ │
│  │           Memory (对话记忆)          │ │
│  └─────────────────────────────────────┘ │
└──────────────┬───────────────────────────┘
               │
     ┌─────────┴──────────┐
     ▼                    ▼
┌──────────┐       ┌───────────┐
│WebSearch │       │ WebScrape │
│  Tool    │       │   Tool    │
└────┬─────┘       └─────┬─────┘
     │                    │
     ▼                    ▼
 搜索引擎API          HTTP + goquery
 (Tavily/SerpApi)    (抓取网页正文)
```

---

## 3. 项目目录结构

```
myagent/
├── main.go                  # 程序入口，初始化 Agent 并启动交互循环
├── go.mod
├── go.sum
├── .env                     # 环境变量（API Key 等，不要提交到 Git）
├── config/
│   └── config.go            # 配置加载（读取 .env、定义配置结构体）
├── agent/
│   └── agent.go             # Agent 初始化、Executor 组装、System Prompt 定义
├── tools/
│   ├── websearch.go         # WebSearchTool 实现
│   └── webscrape.go         # WebScrapeTool 实现
├── memory/
│   └── memory.go            # 对话记忆管理（封装 ConversationBuffer）
├── output/
│   └── formatter.go         # 输出格式化（将 Agent 结果渲染为 Markdown 报告）
└── 开发文档.md               # 本文档
```

---

## 4. 环境准备与配置管理

### 4.1 前置依赖

| 依赖 | 用途 | 安装方式 |
|---|---|---|
| Go 1.21+ | 编程语言 | [官网下载](https://go.dev/dl/) |
| `github.com/tmc/langchaingo` | Agent 框架核心 | `go get github.com/tmc/langchaingo` |
| `github.com/PuerkitoBio/goquery` | HTML 解析 | `go get github.com/PuerkitoBio/goquery` |
| `github.com/joho/godotenv` | .env 环境变量加载 | `go get github.com/joho/godotenv` |

### 4.2 API Key 申请

| 服务 | 用途 | 申请地址 |
|---|---|---|
| OpenAI API (或兼容接口) | LLM 推理 | https://platform.openai.com/api-keys |
| DeepSeek (可选替代) | LLM 推理 (国内友好) | https://platform.deepseek.com/ |
| Tavily API | 搜索引擎 | https://tavily.com/ (每月 1000 次免费) |
| SerpApi (可选替代) | 搜索引擎 | https://serpapi.com/ |

### 4.3 `.env` 文件模板

```env
# LLM 配置
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# 搜索引擎配置
TAVILY_API_KEY=tvly-xxxxxxxxxxxxxxxxxxxx

# Agent 配置
AGENT_MAX_ITERATIONS=5
AGENT_MAX_CONTENT_LENGTH=4000
```

### 4.4 配置结构体 (`config/config.go`)

```go
package config

import (
	"log"
	"os"
	"strconv"

	"github.com/joho/godotenv"
)

type Config struct {
	OpenAIKey        string
	OpenAIBaseURL    string
	OpenAIModel      string
	TavilyKey        string
	MaxIterations    int
	MaxContentLength int
}

func Load() *Config {
	if err := godotenv.Load(); err != nil {
		log.Println("警告: 未找到 .env 文件，将从系统环境变量读取")
	}

	maxIter, _ := strconv.Atoi(getEnvOrDefault("AGENT_MAX_ITERATIONS", "5"))
	maxLen, _ := strconv.Atoi(getEnvOrDefault("AGENT_MAX_CONTENT_LENGTH", "4000"))

	return &Config{
		OpenAIKey:        os.Getenv("OPENAI_API_KEY"),
		OpenAIBaseURL:    getEnvOrDefault("OPENAI_BASE_URL", "https://api.openai.com/v1"),
		OpenAIModel:      getEnvOrDefault("OPENAI_MODEL", "gpt-4o"),
		TavilyKey:        os.Getenv("TAVILY_API_KEY"),
		MaxIterations:    maxIter,
		MaxContentLength: maxLen,
	}
}

func getEnvOrDefault(key, defaultVal string) string {
	if val := os.Getenv(key); val != "" {
		return val
	}
	return defaultVal
}
```

---

## 5. 两日敏捷开发计划

### Day 1：基础设施搭建与 Agent 核心骨架

**目标**：完成项目初始化，跑通 LLM 调用，并实现基础的 ReAct Agent 循环。

* **上午 (4h)：环境搭建与 LLM 接入**
  1. 初始化 Go 项目：`go mod init myagent`
  2. 引入全部依赖（见 4.1 节）
  3. 创建 `.env` 文件，填入 API Key
  4. 编写 `config/config.go`，实现配置加载
  5. 编写 LLM 初始化代码，执行一次简单问答测试连通性
  6. **验收标准**：运行程序后，能在终端看到 LLM 的回复

* **下午 (4h)：定义工具与组装 Agent**
  1. 实现 `tools/websearch.go`（WebSearchTool）
  2. 实现 `tools/webscrape.go`（WebScrapeTool）的框架（先用 mock 数据返回）
  3. 编写 `agent/agent.go`，使用 `agents.Initialize` 组装 ReAct Agent
  4. 在 `main.go` 中编写基础的 CLI 交互循环
  5. **验收标准**：输入"搜索阿里后端面经"，Agent 能调用 WebSearchTool 返回搜索结果

### Day 2：深度内容抓取、业务逻辑完善与测试

**目标**：实现网页内容的深度抓取，优化 Prompt 设定，完成面经的结构化汇总。

* **上午 (4h)：网页抓取与内容清洗**
  1. 完善 `tools/webscrape.go`，实现真实的 HTTP 抓取 + goquery 解析
  2. 实现文本截断逻辑（防止超出 Token 限制）
  3. 添加 `User-Agent` 头和基本的错误处理
  4. **验收标准**：给定一个 CSDN 面经文章 URL，能返回干净的纯文本正文

* **下午 (4h)：Prompt 工程、Memory 与系统集成**
  1. 定制 System Prompt（见第 7 节完整 Prompt）
  2. 集成 `memory.NewConversationBuffer()`，实现多轮对话
  3. 实现 `output/formatter.go`，将结果格式化为标准 Markdown 报告
  4. 端到端测试，调试执行轨迹
  5. **验收标准**：输入"收集腾讯 WXG 后端开发面经"，Agent 完成搜索→抓取→汇总全流程，输出结构化报告

---

## 6. 核心代码实现指南

### 6.1 WebSearchTool (`tools/websearch.go`)

```go
package tools

import (
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
)

type WebSearchTool struct {
	ApiKey string
}

func (t WebSearchTool) Name() string {
	return "web_search"
}

func (t WebSearchTool) Description() string {
	return `在互联网上搜索与后端面经相关的文章。
输入应该是搜索关键词，例如 "字节跳动 Go 后端面经 2024"。
输出是搜索结果列表，包含标题和 URL。
使用建议：组合 "公司名 + 岗位 + 面经" 作为搜索词效果最佳。`
}

func (t WebSearchTool) Call(ctx context.Context, input string) (string, error) {
	apiURL := fmt.Sprintf(
		"https://api.tavily.com/search?api_key=%s&query=%s&max_results=5",
		t.ApiKey,
		url.QueryEscape(input),
	)

	resp, err := http.Get(apiURL)
	if err != nil {
		return "", fmt.Errorf("搜索请求失败: %w", err)
	}
	defer resp.Body.Close()

	body, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", fmt.Errorf("读取响应失败: %w", err)
	}

	var result struct {
		Results []struct {
			Title string `json:"title"`
			URL   string `json:"url"`
		} `json:"results"`
	}
	if err := json.Unmarshal(body, &result); err != nil {
		return "", fmt.Errorf("解析搜索结果失败: %w", err)
	}

	var sb strings.Builder
	for i, r := range result.Results {
		sb.WriteString(fmt.Sprintf("%d. %s\n   URL: %s\n", i+1, r.Title, r.URL))
	}

	if sb.Len() == 0 {
		return "未找到相关结果，请尝试更换关键词。", nil
	}
	return sb.String(), nil
}
```

### 6.2 WebScrapeTool (`tools/webscrape.go`)

```go
package tools

import (
	"context"
	"fmt"
	"net/http"
	"strings"
	"unicode/utf8"

	"github.com/PuerkitoBio/goquery"
)

type WebScrapeTool struct {
	MaxContentLength int
}

func (t WebScrapeTool) Name() string {
	return "web_scraper"
}

func (t WebScrapeTool) Description() string {
	return `读取指定 URL 的网页正文内容。
输入必须是一个合法的 http 或 https URL。
输出是网页的纯文本正文（已去除 HTML 标签）。
注意：如果页面无法访问，会返回错误信息，请尝试其他 URL。`
}

func (t WebScrapeTool) Call(ctx context.Context, input string) (string, error) {
	input = strings.TrimSpace(input)
	if !strings.HasPrefix(input, "http") {
		return "", fmt.Errorf("输入必须是合法的 URL，收到: %s", input)
	}

	client := &http.Client{}
	req, err := http.NewRequestWithContext(ctx, "GET", input, nil)
	if err != nil {
		return "", fmt.Errorf("创建请求失败: %w", err)
	}
	req.Header.Set("User-Agent", "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36")

	resp, err := client.Do(req)
	if err != nil {
		return "", fmt.Errorf("请求页面失败: %w", err)
	}
	defer resp.Body.Close()

	if resp.StatusCode != http.StatusOK {
		return fmt.Sprintf("页面返回状态码 %d，无法读取内容。", resp.StatusCode), nil
	}

	doc, err := goquery.NewDocumentFromReader(resp.Body)
	if err != nil {
		return "", fmt.Errorf("解析 HTML 失败: %w", err)
	}

	// 移除无用元素
	doc.Find("script, style, nav, footer, header, .sidebar, .comment").Remove()

	var sb strings.Builder
	doc.Find("h1, h2, h3, h4, p, li, pre, code").Each(func(i int, s *goquery.Selection) {
		text := strings.TrimSpace(s.Text())
		if text != "" {
			sb.WriteString(text)
			sb.WriteString("\n")
		}
	})

	content := sb.String()

	// 文本截断：防止超出 LLM Token 限制
	maxLen := t.MaxContentLength
	if maxLen <= 0 {
		maxLen = 4000
	}
	if utf8.RuneCountInString(content) > maxLen {
		runes := []rune(content)
		content = string(runes[:maxLen]) + "\n...[内容已截断]"
	}

	if strings.TrimSpace(content) == "" {
		return "页面内容为空或无法提取有效正文。", nil
	}
	return content, nil
}
```

### 6.3 Agent 初始化 (`agent/agent.go`)

```go
package agent

import (
	"context"
	"fmt"
	"log"

	"github.com/tmc/langchaingo/agents"
	"github.com/tmc/langchaingo/chains"
	"github.com/tmc/langchaingo/llms/openai"
	"github.com/tmc/langchaingo/memory"
	lc_tools "github.com/tmc/langchaingo/tools"

	"myagent/config"
	"myagent/tools"
)

const systemPrompt = `你是一个资深的后端技术专家和面试顾问，名叫"面经Scout"。
你的核心任务是帮助用户收集并分析互联网大厂（如阿里、腾讯、字节跳动、美团、百度等）的后端开发面试经验。

## 工作流程
1. 根据用户指定的公司和岗位方向，使用 web_search 工具搜索相关面经文章
2. 从搜索结果中挑选最相关的 2-3 篇文章
3. 使用 web_scraper 工具逐一读取文章内容
4. 综合所有文章内容，生成结构化的面经汇总报告

## 输出格式要求
请严格按照以下 Markdown 格式输出汇总报告：

### 📋 面经汇总：[公司名] - [岗位方向]

#### 一、面试概况
- 来源文章数量与链接
- 面试时间范围
- 面试轮次概况（几轮技术面 + HR面）

#### 二、高频考点 TOP 5
按出现频率从高到低列出，每个考点附带具体面试题示例。

#### 三、详细面试题整理
按知识领域分类（如：编程语言基础、数据结构与算法、数据库、缓存、消息队列、系统设计、项目经验等）

#### 四、面试建议
基于以上面经内容给出的备考建议。

## 注意事项
- 优先搜索近一年内的面经
- 如果某个 URL 无法访问，跳过并尝试其他链接
- 保持客观，忠实于原文内容，不要编造面试题`

func NewExecutor(cfg *config.Config) (chains.Chain, error) {
	llmOpts := []openai.Option{
		openai.WithModel(cfg.OpenAIModel),
		openai.WithToken(cfg.OpenAIKey),
	}
	if cfg.OpenAIBaseURL != "" {
		llmOpts = append(llmOpts, openai.WithBaseURL(cfg.OpenAIBaseURL))
	}

	llm, err := openai.New(llmOpts...)
	if err != nil {
		return nil, fmt.Errorf("初始化 LLM 失败: %w", err)
	}

	agentTools := []lc_tools.Tool{
		tools.WebSearchTool{ApiKey: cfg.TavilyKey},
		tools.WebScrapeTool{MaxContentLength: cfg.MaxContentLength},
	}

	mem := memory.NewConversationBuffer()

	executor, err := agents.Initialize(
		llm,
		agentTools,
		agents.ZeroShotReactDescription,
		agents.WithMaxIterations(cfg.MaxIterations),
		agents.WithMemory(mem),
		agents.WithParserErrorHandler(agents.NewParserErrorHandler(func(s string) string {
			return "解析输出格式出错，请重新组织你的回答。确保遵循 Thought/Action/Action Input 格式。"
		})),
	)
	if err != nil {
		return nil, fmt.Errorf("初始化 Agent 失败: %w", err)
	}

	return executor, nil
}

func Run(ctx context.Context, executor chains.Chain, query string) (string, error) {
	result, err := chains.Run(ctx, executor, query)
	if err != nil {
		return "", fmt.Errorf("Agent 执行失败: %w", err)
	}
	return result, nil
}
```

### 6.4 程序入口 (`main.go`)

```go
package main

import (
	"bufio"
	"context"
	"fmt"
	"log"
	"os"
	"strings"

	"myagent/agent"
	"myagent/config"
)

func main() {
	cfg := config.Load()
	if cfg.OpenAIKey == "" {
		log.Fatal("请设置 OPENAI_API_KEY 环境变量")
	}

	executor, err := agent.NewExecutor(cfg)
	if err != nil {
		log.Fatal(err)
	}

	ctx := context.Background()
	scanner := bufio.NewScanner(os.Stdin)

	fmt.Println("=== 面经 Scout Agent ===")
	fmt.Println("我可以帮你收集和汇总互联网大厂的后端面经。")
	fmt.Println("示例指令：")
	fmt.Println("  - 帮我搜索字节跳动 Go 后端面经")
	fmt.Println("  - 收集阿里巴巴 Java 后端近半年的面经并总结高频考点")
	fmt.Println("输入 quit 退出程序。")
	fmt.Println(strings.Repeat("=", 40))

	for {
		fmt.Print("\n你: ")
		if !scanner.Scan() {
			break
		}
		input := strings.TrimSpace(scanner.Text())
		if input == "" {
			continue
		}
		if strings.EqualFold(input, "quit") || strings.EqualFold(input, "exit") {
			fmt.Println("再见！祝面试顺利！")
			break
		}

		fmt.Println("\n[Agent 思考中...]")
		result, err := agent.Run(ctx, executor, input)
		if err != nil {
			fmt.Printf("执行出错: %v\n", err)
			continue
		}

		fmt.Printf("\n面经Scout:\n%s\n", result)
	}
}
```

---

## 7. System Prompt 设计

完整的 System Prompt 已嵌入 `agent/agent.go` 中的 `systemPrompt` 常量。设计要点：

1. **角色定义**：赋予 Agent "面经Scout" 的角色身份，明确其专业领域。
2. **工作流程**：明确搜索→阅读→汇总三步走流程，避免 Agent 跳过关键步骤。
3. **输出格式**：通过 Markdown 模板约束输出结构，确保每次输出一致、可读。
4. **兜底策略**：要求 Agent 在 URL 不可用时主动跳过，防止卡死。

---

## 8. 输出格式规范

Agent 最终输出的面经报告应符合以下模板：

```markdown
### 📋 面经汇总：字节跳动 - Go 后端开发

#### 一、面试概况
- 参考来源：共 3 篇面经文章
  1. [标题1](URL1)
  2. [标题2](URL2)
  3. [标题3](URL3)
- 面试时间：2024年6月 - 2024年12月
- 面试轮次：一般为 3 轮技术面 + 1 轮 HR 面

#### 二、高频考点 TOP 5
| 排名 | 考点 | 出现次数 | 示例面试题 |
|------|------|----------|-----------|
| 1 | Go 并发编程 | 3/3 | goroutine 泄漏如何排查？channel 底层原理？ |
| 2 | Redis | 3/3 | 缓存击穿/穿透/雪崩区别？Redis 集群方案？ |
| 3 | MySQL 索引 | 2/3 | 联合索引最左匹配原则？explain 各字段含义？ |
| 4 | 系统设计 | 2/3 | 设计一个短链服务？设计一个消息推送系统？ |
| 5 | 网络协议 | 2/3 | TCP 三次握手/四次挥手？HTTPS 握手流程？ |

#### 三、详细面试题整理
**Go 语言基础**
- goroutine 和 thread 的区别是什么？
- GMP 调度模型请详细描述一下
- ...

**数据库**
- MySQL 事务隔离级别有哪些？
- ...

#### 四、面试建议
1. 重点复习 Go 并发编程和 GMP 模型
2. Redis 各种缓存异常场景必须掌握
3. ...
```

---

## 9. 错误处理与日志策略

### 9.1 错误分级

| 级别 | 场景 | 处理方式 |
|------|------|---------|
| **Fatal** | API Key 未配置、LLM 初始化失败 | `log.Fatal()` 直接退出 |
| **Error** | 网页抓取失败 (403/404/超时) | 返回错误描述给 Agent，由 Agent 决定下一步 |
| **Warn** | `.env` 文件缺失、内容截断 | `log.Println()` 记录日志，继续运行 |
| **Info** | Agent 思考过程、工具调用记录 | 可选打印到终端，便于调试 |

### 9.2 Tool 层错误处理原则

* Tool 的 `Call()` 方法中，**业务性错误**（如网页 404）应作为正常字符串返回给 Agent，而非 Go error。这样 Agent 可以"看到"错误并决定尝试其他链接。
* 只有**系统性错误**（如网络完全不通、JSON 解析失败）才返回 Go error。

```go
// 业务性错误 - 返回 string，Agent 可以处理
if resp.StatusCode == 403 {
    return "该网站拒绝访问(403)，请尝试其他 URL。", nil
}

// 系统性错误 - 返回 error
if err != nil {
    return "", fmt.Errorf("网络请求失败: %w", err)
}
```

### 9.3 Agent 执行过程日志

开发调试期间，可通过设置 `langchaingo` 的 verbose 模式查看 Agent 的完整思考链：

```go
executor, err := agents.Initialize(
    llm,
    agentTools,
    agents.ZeroShotReactDescription,
    agents.WithMaxIterations(5),
    agents.WithCallbacksHandler(callbacks.LogHandler{}),  // 打印执行日志
)
```

---

## 10. 测试策略

### 10.1 单元测试

针对每个 Tool 编写独立测试：

```go
// tools/webscrape_test.go
func TestWebScrapeTool_Call(t *testing.T) {
	tool := WebScrapeTool{MaxContentLength: 500}
	ctx := context.Background()

	// 测试正常页面
	result, err := tool.Call(ctx, "https://example.com")
	assert.NoError(t, err)
	assert.NotEmpty(t, result)

	// 测试非法 URL
	_, err = tool.Call(ctx, "not-a-url")
	assert.Error(t, err)

	// 测试内容截断
	result, err = tool.Call(ctx, "https://some-long-article.com")
	assert.NoError(t, err)
	assert.LessOrEqual(t, utf8.RuneCountInString(result), 520) // 500 + 截断提示
}
```

### 10.2 集成测试用例

| 编号 | 测试场景 | 输入 | 预期结果 |
|------|---------|------|---------|
| E2E-1 | 基础搜索 | "搜索阿里 Java 面经" | Agent 调用 web_search，返回搜索结果列表 |
| E2E-2 | 完整流程 | "收集字节跳动 Go 后端面经并总结" | Agent 完成搜索→抓取→汇总，输出结构化报告 |
| E2E-3 | 抓取失败容错 | 提供一个无效 URL | Agent 识别错误，尝试其他链接 |
| E2E-4 | 多轮对话 | 先搜索，再追问 "上面的 Redis 题目详细讲讲" | Agent 基于 Memory 理解上下文，给出回答 |
| E2E-5 | 边界情况 | "搜索一个不存在的公司的面经" | Agent 返回"未找到相关结果"的友好提示 |

### 10.3 运行测试

```bash
# 单元测试
go test ./tools/... -v

# 全部测试
go test ./... -v
```

---

## 11. 难点与解决方案 (避坑指南)

### 11.1 反爬虫机制

* **问题**：牛客网、知乎等网站有严格的反爬机制，直接 HTTP GET 可能会被拦截 (403 Forbidden)。
* **方案**：
  * 在 `WebScrapeTool` 中添加常见的 `User-Agent` 头
  * 如果时间允许，可以引入无头浏览器（如 `github.com/chromedp/chromedp`）来渲染 JS 页面
  * **两天内最稳妥的做法**是优先抓取 CSDN、博客园或 GitHub 上的面经仓库

### 11.2 Token 超限

* **问题**：抓取多篇面经后，文本量巨大，容易超出 LLM 的上下文窗口。
* **方案**：
  * 在 `WebScrapeTool` 返回结果前，强制截断文本（通过 `MaxContentLength` 配置控制）
  * 如果需要处理更多文章，可以让 Agent 分批次阅读并自行做局部总结（Map-Reduce 思路）

### 11.3 Agent 死循环

* **问题**：Agent 可能会反复调用同一个工具而得不到新结果。
* **方案**：
  * 务必设置 `agents.WithMaxIterations(5)` 来限制最大思考步数
  * 在 Tool 的 `Description` 中明确告知 Agent 错误处理方式（如"如果抓取失败，请尝试搜索其他链接"）

### 11.4 输出格式不稳定

* **问题**：Agent 的 ReAct 输出格式有时不符合解析器预期，导致 `parsing error`。
* **方案**：
  * 使用 `agents.WithParserErrorHandler()` 注册自定义的解析错误处理器
  * 在错误处理器中提示 Agent 重新按格式输出

### 11.5 中文编码问题

* **问题**：部分网页的字符编码不是 UTF-8，导致抓取后出现乱码。
* **方案**：
  * 检测响应头中的 `Content-Type` charset
  * 使用 `golang.org/x/text/encoding` 包进行编码转换

---

## 12. 扩展性设计

本项目采用 `tools.Tool` 接口解耦工具层，未来可以方便地新增数据源和功能：

| 扩展方向 | 实现方式 | 优先级 |
|---------|---------|-------|
| 新增数据源 (如 LeetCode 讨论区) | 实现新的 Tool，注册到 Agent | 中 |
| 面经本地持久化 | 添加 `FileSaveTool`，将汇总结果保存为 .md 文件 | 高 |
| 知识库检索 (RAG) | 引入向量数据库，将历史面经 Embedding 后存储 | 低 |
| Web UI | 使用 `net/http` 或 Gin 提供 HTTP API + 前端页面 | 低 |
| 支持更多公司/岗位 | 纯 Prompt 层面调整，无需改代码 | 高 |
